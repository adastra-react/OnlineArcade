'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var react = require('react');

var hasFocus = function hasFocus() {
  return typeof document !== 'undefined' && document.hasFocus();
};

var useWindowFocus = function useWindowFocus() {
  var _useState = react.useState(hasFocus),
      focused = _useState[0],
      setFocused = _useState[1]; // Focus for first render


  react.useEffect(function () {
    setFocused(hasFocus()); // Focus for additional renders

    var onFocus = function onFocus() {
      return setFocused(true);
    };

    var onBlur = function onBlur() {
      return setFocused(false);
    };

    window.addEventListener('focus', onFocus);
    window.addEventListener('blur', onBlur);
    return function () {
      window.removeEventListener('focus', onFocus);
      window.removeEventListener('blur', onBlur);
    };
  }, []);
  return focused;
};

exports.default = useWindowFocus;
//# sourceMappingURL=use-window-focus.cjs.development.js.map
